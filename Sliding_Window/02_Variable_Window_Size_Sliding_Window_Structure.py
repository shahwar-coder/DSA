start = 0

for end in range(len(arr)):
    # expand window by including arr[end]

    while condition is violated:
        # shrink window from the left
        start += 1

    # window [start, end] is currently valid
